export type TCharacters = {
  id: number;
  image: string;
  name: string;
  status: string;
  gender: string;
  species: string;
};
export type IInfo = {
  pages: number;
  next: string;
  prev: string;
};
